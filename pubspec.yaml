name: swipe_back
description: iOS-style full screen back swipe gesture for Flutter. Enables swiping from anywhere on the screen to navigate back, not just the left edge.
version: 0.0.2
homepage: https://github.com/speakblend/swipe_back
repository: https://github.com/speakblend/swipe_back
issue_tracker: https://github.com/speakblend/swipe_back/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
